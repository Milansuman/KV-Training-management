services:
  postgres:
    image: postgres:latest
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_DB=train3
    ports:
      - 5432:5432
    volumes:
      - train3_data:/var/lib/postgresql

volumes:
  train3_data:
